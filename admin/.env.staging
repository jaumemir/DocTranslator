# Staging environment custom environment variables (must start with VITE_)

## Backend API public path (if using CORS for cross-origin, full path is needed)
VITE_BASE_API = "/api"

## Router mode: hash or html5
VITE_ROUTER_HISTORY = 'history'

VITE_PUBLIC_PATH = '/'
